# Copy to server/.env.local and set the real key
GEMINI_API_KEY=REPLACE_WITH_YOUR_GEMINI_KEY
# Single or comma-separated client API keys used by clients for server auth
API_KEY=local_dev_key_here
# API_RATE_WINDOW_MS and API_RATE_MAX control rate limiting (window in ms, max requests per window)
API_RATE_WINDOW_MS=60000
API_RATE_MAX=20
# Optional: set REDIS_URL to enable a Redis-backed rate limiter (e.g. redis://:password@redis:6379/0)
REDIS_URL=
# Optional: comma-separated allowed frontend origins for CORS (default: http://localhost:3000)
FRONTEND_ORIGINS=http://localhost:3000
PORT=4000
