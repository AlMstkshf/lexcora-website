{
  "name": "lexcora-server",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p ."
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^5.2.1",
    "stripe": "^16.12.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.14",
    "@types/express": "^5.0.6",
    "ts-node-dev": "^2.0.0",
    "typescript": "~5.8.2"
  }
}
